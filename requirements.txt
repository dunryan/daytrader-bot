# ── Core data / numerics ──
pandas>=2.2,<3.0
numpy>=1.26,<3.0

# ── Market data & broker (Alpaca) ──
alpaca-py>=0.33,<1.0

# ── News / sentiment ──
requests>=2.32,<3.0

# ── Scheduling & service lifecycle ──
APScheduler>=3.10,<4.0

# ── Configuration & validation ──
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0,<2.0
PyYAML>=6.0,<7.0

# ── Persistence ──
SQLAlchemy>=2.0,<3.0

# ── Market calendar ──
pandas-market-calendars>=4.4,<6.0

# ── Reporting (Module 6) ──
matplotlib>=3.8,<4.0
reportlab>=4.1,<5.0

# ── Dev / test ──
pytest>=8.2,<9.0
